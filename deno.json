{
  "tasks": {
    "dev": "deno run --allow-net --allow-read server.ts",
    "build": "deno run -A build.ts",
    "preview": "deno run -ENR jsr:@std/http/file-server dist/",
    "test": "npx playwright test"
  }
}
